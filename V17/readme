通过两个版本完成动态页面的实现
此版本完成最原始的方式实现动态页面

动态页面:含有动态数据的页面，这样页面不是事先准备好的某个.html文件，而是伴随请求每次都由程序
        临时生成的页面。
静态页面:事先准备好的写死在.html文件中的页面

需求:用户点击超链接查看当前网站所有注册用户信息。要在一个页面上显示user.dat文件中的所有 用户
这个页面不能是事先准备好的，因为随着时间推移，user.dat文件会伴随用户的不同操作一直变化(注册、修改）
因此，这个页面上的数据要随时变化，对此这样的页面只能在每次请求时由程序读取user.dat文件后根据文件
内容来临时生成才能时刻体现最新数据。